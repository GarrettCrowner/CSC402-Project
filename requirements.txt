# Existing dependencies
flask>=3.0.3
openai>=1.35.0
requests>=2.32.3
beautifulsoup4>=4.12.3
python-dotenv>=1.0.1

# Qdrant vector database integration
qdrant-client>=1.9.0
sentence-transformers>=2.7.0

# PDF ingestion (used by qdrant_setup.py — optional for URL-only deployments)
pymupdf>=1.24.0
