## Uploading to MinIO for First Time
1. after following instruction to run MinIO, open MinIO in browser (http://localhost:9001)
2. create new bucket called 'documents'
3. download the PDFs folder
4. hit upload file, not folder to prevent any issues
5. select all pdfs within the PDFs folder you just downloaded
